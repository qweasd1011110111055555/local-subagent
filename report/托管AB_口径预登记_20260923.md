# 托管空回执 A/B 口径预登记（20260923，跑前写死；结果出来前不改判读）

## 路由混杂与臂语义变更（用户 20260923 告知）

DeepSeek 已把 v4-flash 请求全量路由到 v4.1 ⇒ 托管 `deepseek-flash` 别名 believed=v4.1（非 v4-flash-0731）。
因此 HF 臂语义从「同快照跨 harness（分离 H3/H4）」改为「**客户端恒定、代际变化**」。
能力层对照改由 **HG 臂**承担：第三方供应商、用户断言智能评分明确低于 v4-flash-0731 的更前一代（如 GLM 5.2）。
**凭据**：代理每回合记录上游回包 `model` 字段（线上回包自报，写入 console log）。

## 三臂问题（跑前冻结）

| 臂 | 端点/模型 | 回答的问题 |
|---|---|---|
| HF | 托管 `deepseek-flash`（believed v4.1） | 我们的 ds4f_loop 客户端对**现行代**托管模型能否交出活 ⇒ **采购决策 pivotal**（升 v4.1+加机器能否修好管线） |
| HP | 托管 `deepseek-v4-pro`（未被路由波及） | 更高档位在客户端下的行为（tier 敏感性旁证） |
| HG | 第三方弱代模型（GLM 5.2，待 key） | **能力层对照**：更弱的模型在同一客户端下若能交活 ⇒ 能力出局、首疑=本地 DGX serving 栈 |

## 判读矩阵（跑前冻结；本地参照 = B1/B2b/B2w 三跑 v4-flash-0731@本地 全零产出）

- HF 成 + HG 成 ⇒ 客户端与任务书族无毒；本地失败定位在**模型侧**（本地 serving 栈列首疑——vLLM
  function-calling 模板/回执裁剪/think token 处理）；「买机器」降级为「先在 DGX 部署 v4.1 小测本任务族」。
- HF 成 + HG 败 ⇒ 存在能力阈值；v4.1×客户端可用 ⇒ 升级有望修管线，但本地 v4.0 失败仍无法在
  {代际更聪明, 本地栈坏} 间分离（需本地部署 v4.1 小测）。
- HF 败 ⇒ 客户端/任务书族对跨供应商跨代际模型一致产空 ⇒ **升级硬件无助于空回执，先修脚手架**（H4a/c/H5 主嫌）。
- HG 臂归因边界：第三方模型 serving 栈与 DeepSeek 不同 ⇒ HG 成功只排除「客户端+任务族对一切模型有毒」，
  **不能**单独定位到「本地 vLLM 模板特定坏」；定位永远靠 HG+HF 组合，不靠单臂。

## 客户端平权（与本地 B2b 臂逐项同）

同一任务书（仅路径段 b2run→b2run_hosted_<arm> 改写，其余逐字节同，当场断言过）、同一 ds4f_loop 真代码
（只 patch URL/MODEL 两常量，照 loop_under_stub 僧例）、同预算（--max-turns 12 / 默认 8192 / temp 0）、
**不取 --lock**（托管不打共享 DGX）。每臂独立 workdir+任务书（防 B2_rows.jsonl 跨臂追加）。

## 验收程序（不变）

`b2_verify.py` + 地板逐位 + trace 事后污染扫描（floor/verify/仪器路径零命中）+ usage 成本汇总（代理行与 trace 双源）；
报告拒绝覆写落 `托管臂空回执AB报告_<时间戳>.txt`；空回执四态（EMPTY/TRUNCATED/答错/协议失败）分开计数不合并。

## HG 臂待办

等用户给 key（+若非标准端点：base URL 与模型 ID）。启动器届时加 `--upstream/--key-file` 两参（测试侧文件，不改生产件）；
先冒烟（正例+坏模型 4xx 负对照）再跑。

## 跑后补记（20260923 14:27，结果落盘后追加；以上冻结部分一字未动）

实际结果=「HF 败 + HP 形式败实质成」，矩阵未预登记此格；判读按 HF 败行执行 + HP 单列（未回改冻结件）。
HF rc=5（思维链 23,853 字撞 8192，零交付）；HP rc=3 但 B2_rows.jsonl 已写完 = **564/564 逐位全中**
（b2_verify rc=0）。HG 臂职能（能力对照）已被 HP 实质交付回答 ⇒ 不再跑、不再要 key。
详见 `ds4f/托管臂空回执AB报告_20260923-1427.txt`。

## 手动臂补登记（20260923，用户开窗实测前写死）

背景：用户质疑「托管 flash 空回执」与其日常经验相悖（v4.1-flash 在别的 agent 窗口从不出此象），
主动提出亲自开窗实测。此臂 =「**同任务书 × 用户的 harness**」，补托管 A/B 缺的那格；用户自发，
非冻结判定的一部分。

- 包：`C:/Users/18016/Downloads/ds4f/b2_manual_20260923/`（src sha 级同源；任务书仅
  `ab_deleg/b2run`→`b2_manual_20260923` 路径段改写 ×7，其余逐字节同，且改写后零 `ab_deleg` 指针；
  目录故意放 ab_deleg 之外）。纪律：**窗口不许读任何 `*floor*`/`b2_verify`/托管AB 报告**（答案钥匙）；
  验收 = 用户报完成后，我对 `b2_manual_20260923/B2_rows.jsonl` 跑 b2_verify 对 B2_floor.json。
- 预测（跑前写死）：
  - **P1（最可能）正常交付** ⇒ harness/包络归因成立——同任务书在框架自管思维链/续写/脚本习惯的
    harness 下不是毒物，与用户日常经验一致。同时记录窗口走的办法：直接写脚本 = B2c 路线复现
    （恰是三问尺 Q2 的答案）；逐段判读 = 包络管理差异路线。
  - **P2（意外）同样空/截断** ⇒ 任务书本身跨 harness 毒性成立 ⇒ 书写法结论加重；
    用户历史成功 = 别的任务形。
- 混杂声明：本臂非盲、温度非 0、用户 harness 自带脚本/续写习惯 ⇒ 它回答「这书在真实使用环境
  可用否」，**不**回答「ds4f_loop 预算参数是否有罪」（后者已由托管 A/B 回答）。

## 手动臂·DGX 直连版补登记（20260923，用户提出，开窗前写死）

用户另可直连 DGX（:8888，deepseek-v4-flash-0731）把同一模型当主 agent。此臂 =
「**同机同快照（v4-flash-0731）× 用户 harness**」——全场最干净的一格：结果无需任何
代际/栈上诉即可归因。

- 包：`C:/Users/18016/Downloads/ds4f/b2_manual_dgx_20260923/`（构造同上，路径段改写 ×7，
  语料 sha 同源，独立 workdir 防跨臂覆写）。纪律同前：窗口零接触 `*floor*`/`b2_verify`/报告。
  验收 = 对 `b2_manual_dgx_20260923/B2_rows.jsonl` 跑 b2_verify 对 B2_floor.json。
- DGX 礼仪（硬约束）：开窗前用户通知同事（并发=1 共享）+ 探隧道；窗口串行请求，
  单场量级与 ds4f_loop 一场相当（约 3 万–9 万 tokens）。
- 请用户记录（读判据）：①窗口实际指向的模型 ID；②适配层可见的 max_tokens / 思维模式设置
  （看不到就记「未知」，诚实缺口）；③成功则记它走的办法（脚本 vs 逐段判读），失败则贴
  失败形态（空/截断/工具协议错）。
- 预测（跑前写死）：
  - **P1′（最可能）交付** ⇒ 本地 vLLM 栈实测无罪 + v4-flash-0731 快照本体无罪 ⇒
    ds4f_loop 包络定罪到最大纯度（同模型同机只换 harness）；且直接证明「现有 DGX 设施
    修 harness 即可用」，采购问题彻底闭环。
  - **P2′（意外）同样空/截断** ⇒ 本地 serving 栈（vLLM 思维链/工具模板处理）重新成为
    首疑——托管复刻只证明失败会复刻，不证明好 harness 下本地栈也能成；同时用户日常
    直连用法若一直成功，则其适配层必有我们不知道的关键设置（追查点）。
- 两手动臂分工：DGX 臂答「现有设施可否修好」（贴本项目决策）；托管 v4.1 臂答「升代
  是否有解」（贴采购备选）。只跑一个的话优先 DGX 臂。
