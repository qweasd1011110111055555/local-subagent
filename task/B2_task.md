任务编号：20260920-AB-B2
目标：在 2 份「立项指南」通知里，对**每一处**「加收」逐处判定它属于哪一类，
并（对「规定」类）摘出它规定的**上限数值**。**只交一张 ASCII 判定表**——
中文一个字都不用你打（原文片段由装配脚本自己取）。

路由元数据：脚本一发=否 盲审=否 搬原文=是 对外=否 需上下文=否

预估轮次：40

**只读边界**：本次只许读**工作目录内**的文件（工作目录 = `…/ab_deleg/b2run`，语料在 `src/`）。
目录外的任何文件都不要读、不要改、不要运行——需要的东西任务书里都给全了。

待读（都在工作目录下，绝对路径）：
- `C:/Users/18016/Downloads/ds4f/ab_deleg/b2run/src/b01.txt`（2859 行 / **299 处**「加收」）
- `C:/Users/18016/Downloads/ds4f/ab_deleg/b2run/src/b02.txt`（2170 行 / **265 处**「加收」）

语料是**硬换行的表格转文本**：一个表头或一个单元格会被切成许多短行（例如
`…（加收） 次 可加收不超过` 与 `100%。 甲类` 分在两行）。⇒ 判一处时**要看它上下几行**，
不能只看它所在的那一行。

输出写到：C:/Users/18016/Downloads/ds4f/ab_deleg/b2run/B2_rows.jsonl

**写法（照做，别改成「先通读、再统一写」）**：一份一份过，**每读一段就立刻把这一段的条目追加进产出文件**，
再读下一段（读一段 ⇒ 追加一段 ⇒ 再读）。**不许先读完两份再动笔**——
上一版就是这么写的：读到第 11 轮思维链吃满额度、一个条目都没落盘。
建议每次读 **300~400 行**（约 4~6 千字符），判完这一段就追加。

硬性要求：

1. **一处不许漏**。要报的条数＝该文件里「加收」在**同一行内**出现的总次数，
   已知：`b01.txt` = **299** 处、`b02.txt` = **265** 处、合计 **564** 处。
   自己数一遍核对（对不上就是数错了，先查清楚再往下走）：
   `grep -n -o "加收" C:/Users/18016/Downloads/ds4f/ab_deleg/b2run/src/b01.txt | wc -l`
   全部追加完后，产出文件的行数也必须是 564：`wc -l C:/Users/18016/Downloads/ds4f/ab_deleg/b2run/B2_rows.jsonl`
2. 每处判一个类别 `cls`：
   - `R` = 该处落在一条**加收规定**里（明示可以加收、或给出加收上限）
   - `T` = 该处只是**项目名 / 字段名 / 表头**的一部分（典型：紧贴着「（加收）」）
   - `O` = 其他（总则、术语定义、叙述性提及、目录、页码、页眉…）
3. `cls=R` 时再给该规定里的**上限数值**：`lim` = 纯数字，`unit` = `pct`（百分号）或 `yuan`（元）。
   规定里没有数值 ⇒ `lim` 与 `unit` 都填空字符串 `""`。**非 R 一律填空字符串。**
   **数值必须逐字照抄**，不许自己算、不许四舍五入、不许换算单位。
4. **每判完一段立刻追加**，不要攒到最后。每条一行 JSON、**全 ASCII、不带中文**：
   `printf '%s\n' '{"f":"b01.txt","ln":63,"occ":2,"cls":"R","lim":"100","unit":"pct"}' >> C:/Users/18016/Downloads/ds4f/ab_deleg/b2run/B2_rows.jsonl`
   - `f` 只许 `b01.txt` 或 `b02.txt`；`ln` = 行号（1 基）；`occ` = **该行内第几处**「加收」（1 基）。
   - **行的顺序必须与语料顺序一致**（先 b01 按行号升序、再 b02 按行号升序）。
5. 办法自便（grep / sed / python 都行），但**必须有依据**：拿不准的那一处宁可判 `O`，
   也不许编一个看上去合理的类别或数值。

回执（≤200 字；**不许复述推理过程、不许把判定表贴进回执**）：
每份文件报来的条数、自己 grep 数出来的数、`wc -l` 的行数、你用了什么办法。
末行固定格式：`完成：b01=.. b02=..；合计 N 条；装配失败 0 条`
